# ArraySplitter regression baseline — post-iter-5 schema (locked 2026-05-21)
#
# Layered history (each entry below is byte-stable against the prior step
# unless marked with *):
#
#   01500ba (iter-2)  baseline: triple_accel SIMD ED across the codebase.
#                       behaviour-preserving; output bytes identical to e23932c.
#
#   718cf74 (iter-5)  intermediate HORs as first-class rows in hors.tsv:
#                       *.hors.tsv      gains `level` + `parent_idx` columns
#                                       and rows of type=sub_hor for level>=2.
#                       *.monomers.tsv  `parent_idx` now references the
#                                       deepest enclosing hors.tsv row;
#                                       new `parent_level` column reports
#                                       which level that is.
#                        .decomposed.fasta, .lengths, .summary.tsv unchanged.
#
#   ecf4fcd (no_period) .summary.tsv gains a trailing `status` column
#                       ({ok | no_period}); no_period marks the degenerate
#                       "period == array length" fallback (paper review #6).
#                       Column VALUES of every prior field are byte-identical;
#                       the other 4 output files are unchanged (verified: their
#                       md5s below are untouched).
#
#   44618a8 (iter-6)    peer review bug fixes: parent_idx off-by-one fix,
#                       subsampling de-aliasing via SplitMix64 stratified jitter,
#                       canonical orientation tie-break, and Option<usize> ED.
#                       - parent_idx correctly references enclosing top-level
#                         monomer in hors.tsv even with left flanks (100% clean joins).
#                       - subsampling de-aliasing adjusts chr2_mat/pat period from
#                         695 to true peak 697; hor_autocorr deltas < 0.018.
#
# Input:   test_data/zebra_finch_satdna.fasta (60.01 Mbp, 429 arrays)
# Command: arraysplitter -i test_data/zebra_finch_satdna.fasta \
#                        -o <prefix> -t 4 --method autocorr
#
# When a future brief intentionally changes output schema, regenerate this
# file in the SAME commit that changes the schema. tests/regression/run.sh
# diffs the binary's output against the rows below.
#
# Columns: extension<TAB>md5<TAB>size_bytes<TAB>line_count
.decomposed.fasta	d013efdb4c846614af079546898eb3fa	60488876	858
.lengths	f507a57087175479194c104ff6186a1a	1381261	858
.summary.tsv	c54905ca645ec96802be3eb34c343e76	1653833	430
.hors.tsv	d19438efaacdf101a4efe2d48cbabca4	106773764	444346
.monomers.tsv	3b3606c92335ea99d1c022276766dfa1	114959237	511366
